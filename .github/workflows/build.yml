name: Build

on:
  push:
    # TODO : Remove ci-build branch after the workflow is verified to work correctly
    branches: [master, main, ci-build]
  pull_request:
    branches: [master, main]

jobs:
    build-cmake:
      strategy:
        fail-fast: true
        matrix:
            os: [ubuntu-latest, windows-latest]
            preset: [default]
            python-version: [3.11]
      runs-on: ${{ matrix.os }}
      steps:
        - uses: actions/checkout@v6

        - uses: actions/setup-python@v6
          with:
            python-version: ${{ matrix.python-version }}
        
        - name: Setup CCache
          uses: hendrikmuhs/ccache-action@v1.2
          with:
            key: ${{ github.job }}-${{ matrix.os }}

        - name: Setup pybind11
          run: pip install pybind11

        - name: Configure
          shell: bash
          run: |
            export CMAKE_PREFIX_PATH=$(python -m pybind11 --cmakedir)
            cmake --preset=${{ matrix.preset }}-ci

        - name: Build
          shell: bash
          run: cmake --build --preset=${{ matrix.preset }}-ci

        - name: Test
          shell: bash
          run: ctest --preset=${{ matrix.preset }}-ci

        - name: Package
          # TODO : The binary directory here is hardcoded.
          shell: bash
          run : |
            cmake --install build-ci --prefix dist

        - name: Upload artifact
          uses: actions/upload-artifact@v4
          with:
            name: novaphy-${{ matrix.os }}-cmakestandalone
            path: dist/

    build-wheel:
      strategy:
        fail-fast: true
        matrix:
            os: [ubuntu-latest, windows-latest]
            preset: [default]
            python-version: [3.11]
      runs-on: ${{ matrix.os }}
      steps:
        - uses: actions/checkout@v6

        - name: Set up Python ${{ matrix.python-version }}
          uses: actions/setup-python@v6
          with:
            python-version: ${{ matrix.python-version }}

        - name: Install Python build dependencies
          run: pip install scikit-build-core pybind11 numpy pytest build

        - name: Setup CCache
          uses: hendrikmuhs/ccache-action@v1.2
          with:
            key: ${{ github.job }}-${{ matrix.os }}

        - name: Build and install
          env: 
            CMAKE_ARGS: "--preset=${{ matrix.preset }}-ci"
          run: pip install . -C build-dir=build-ci -v

        - name: Run tests
          run: pytest tests/python/ -v

        - name: Package wheel
          env:
            CMAKE_ARGS: "--preset=${{ matrix.preset }}-ci"
          run: python -m build --wheel --outdir dist -C build-dir=build-ci

        - name: Upload artifact
          uses: actions/upload-artifact@v4
          with:
            name: novaphy-${{ matrix.os }}-python${{ matrix.python-version }}
            path: dist/*